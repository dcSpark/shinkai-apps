import { createAction } from '@reduxjs/toolkit';

// export const nodeConnectionSuccedeed = createAction<NodeConnectionData>('node/connect/succeeded');
// export const nodeConnectionError = createAction<Error>('node/connect/error');
